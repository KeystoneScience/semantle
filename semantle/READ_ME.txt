~~Welcome to the Semantle React Native App!~~

In order to run this package, you need to have expo installed.
https://docs.expo.dev/get-started/installation/

After that, run
`expo install`
to get all the required packages installed,
and then run
`expo start`
to start the app.



